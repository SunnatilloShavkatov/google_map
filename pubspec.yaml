name: google_map_1
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.0.6
  geocoding: ^2.0.0
  #geo location
  geolocator: ^7.4.0
  #
  fluttertoast: ^8.0.8
  #location
  location: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/png/
    - assets/